void main() {
    const int year = 2026;
    nextYear := 2027;

    string name = "Hello";
    otherName := "World"; // go-style attribution

    //println(name);
    // fullName := sprintf("${name} ${otherName}")
}