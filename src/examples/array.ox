void main() {
    int array[] = [1,2,3,4,5];

    array.append(6);      // [1, 2, 3, 4, 5, 6]
    array.pop();          // [1, 2, 3, 4, 5]
    array.length;         // 5
    array.contains(3);    // true
}